import { SortNumbersPipe } from './sort-numbers.pipe';

describe('SortNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
